{
  "name": "unraid-modernui",
  "version": "0.8.0",
  "private": true,
  "description": "Clean, flat, responsive theme for Unraid 7.x. v0.5.8: Docker page gains a floating update-progress panel (container name + byte-weighted overall % + inferred download speed, persisted across navigation via sessionStorage) and a one-click \"Update all\" action for every container with an available update. UX fixes: the folder default Expanded/Collapsed control no longer inverts manually-toggled folders, and the row/tag-modal checkbox tick is now mathematically centered (inline SVG). CI runs the TS test suite + asset build on every PR.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "package": "node tools/package-txz.mjs",
    "dev-mirror": "node tools/dev-mirror.mjs",
    "test": "npm run test:ts && npm run test:php",
    "test:ts": "vitest run",
    "test:php": "node tests/unit-php/run-all.mjs",
    "test:integration": "node tests/integration/install-uninstall.mjs",
    "lint": "npm run lint:js && npm run lint:php",
    "lint:js": "biome ci",
    "lint:php": "node tools/php-cs-fixer.mjs --check",
    "format": "npm run format:js && npm run format:php",
    "format:js": "biome check --write",
    "format:php": "node tools/php-cs-fixer.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "jsdom": "^24.0.0",
    "sass": "^1.77.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "lit": "^3.3.3"
  }
}
